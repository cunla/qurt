package com.emc.qurt.sampling;

/**
 * Created by morand3 on 12/24/2014.
 */
public class Tools {
//    public static List<VirtualMachineData> convertFalToSql(DatabaseInfo info) {
//        if (null == info) return null;
//        List<VirtualMachineData> data = new LinkedList<>();
//        DateTime sampleDate = info.getSampleDate();
//        if (null != info.getClusterNames()) {
//            for (Cluster cluster : info.getClusterNames()) {
//                String clusterName = cluster.getClusterName();
//                for (VmData vmData : cluster.getVmData()) {
//                    VirtualMachineData a = new VirtualMachineData(sampleDate,
//                            clusterName, vmData.getVmName(), vmData.getVmState(), vmData.isLocal());
//                    data.add(a);
//                }
//            }
//        }
//        return data;
//    }
}
